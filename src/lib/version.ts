/**
 * App version stamp. Bumped in the same commit as every zip drop so we can
 * verify at-a-glance which build is live. Rendered in the Masthead utility
 * strip.
 */
export const APP_VERSION = "v6.39";
